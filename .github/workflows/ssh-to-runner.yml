name: CI
on: [push]
jobs:
  build:
    # Пусть лежит как пример
    if: false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
